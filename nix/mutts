#!/bin/bash

source $HOME/Dropbox/config/bash/bash.io.sh

# Variables
sensitive=$(cat<<EOF
$HOME/.netrc.gpg
$HOME/.msmtprc.gpg
$HOME/.imapfilter/esc.gpg
$HOME/.mutt/.mutt.passwords.gpg
EOF
)

function ctrl-c() { nightman return ${sensitive}; }
trap ctrl-c SIGINT		# Trap exits and interrupts to
trap ctrl-c EXIT		# avoid leaving behind secrets

nightman checkout ${sensitive}

# Run target
export LOCAL_CONFIG='esc'
mutt
